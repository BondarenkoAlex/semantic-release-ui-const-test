const colors = require('./colors');

const BORDER_COLORS = {
    // Цвет визуального разделения - секции в формах/модалках, списках(типа DefinitionList)
    separateBorderRegular: colors.minorGrayLighten2,
}

module.exports = BORDER_COLORS;